# declmig-interfaces

Data-only contracts for JSON Schema driven SQL migrations shared by web and API servers. Schema: `schema/v1`. Generated TypeScript and Dart live under `generated/` and must stay types-only.
