[package]
name = "declmig-interfaces"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Data-only interfaces for Declarative Migrations"
repository = "https://github.com/declarative-migrations/declmig-interfaces"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[lints.rust]
unsafe_code = "forbid"
