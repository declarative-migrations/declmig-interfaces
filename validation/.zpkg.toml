[package]
org = "declarative-migrations"
name = "declmig-public-validation-contracts"
version = "0.1.0"
description = "Public peer validation authorities for declarative migrations"
license = "MIT"

[targets.json-schema]
dir = "."
adapter = "none"

[targets.typespec]
dir = "typespec"
adapter = "none"
